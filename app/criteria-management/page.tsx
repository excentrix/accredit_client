import { CriteriaManager } from "@/components/template-management/criteria-manager";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="w-full h-screen flex ">
      <CriteriaManager />
    </div>
  );
};

export default page;
